import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
	root: {
		margin: '1rem auto',
		display: 'flex',
		alignItems: 'flex-start'
	},
	movieListItemTitle: {
		flexGrow: 1
	}
}));
